#include "td_math.h"

//Returns the max between two numbers
int max(int x, int y){
    if(x > y){
        return x;
    }
    return y;
}

//Returns the min between two numbers
int min(int x, int y){
    if(x < y){
        return x;
    }
    return y;
}