//Update With Sourcefiles
#include <stdio.h>
#include <unistd.h>

#include "game.h"

int main(){
    //char end = 'a';
    //while(end != 'q'){
    //    end = game_start();
    //    if(end == 'd'){printf("\n%s", "You have Died."); sleep(2); }
    //}
    game_start();
    game_end();
}